## 一、产品定位与名称（新架构版）

- **生态名称**：「ArkMatrix 方舟矩阵」
- **核心智能体群**：
  - **系统超级Agent**：小艺Claw（系统级，可调用所有生态的 SKILL）
  - **方舟矩阵自有Agent**：部署于云端的领域大脑，直接掌握矩阵内所有应用的全部数据和操作能力，并对外开放一组原子化的旅行管理 Skill
  - **端侧助手**：《方舟旅迹》内置的小艺Agent（系统普通权限，仅能与方舟旅迹交互，但通过 A2A 协议与云端方舟Agent实时联动）
- **一句话价值**：
  以**方舟矩阵的云端共享数据体**为底座，由**系统级小艺Claw进行跨生态意图编排**，结合**方舟旅迹的原子旅行技能**与**云端方舟Agent的深度理解**，用自然语言一站式规划、自动执行、实时守护整段旅程，真正打破三方与系统应用的数据孤岛。

---

## 二、生态矩阵分层与对外开放 Skill 集（重构版）

| 生态层级       | 应用       | 对外开放的关键 SKILL（供小艺Claw调用）                                                                                                                                                                                                                             | 数据与权限说明                                                                                          |
| ---------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **系统生态应用** | 备忘录      | `search_notes`、`get_checklist`、`create_note`                                                                                                                                                                                                         | 系统级数据，小艺Claw直调，本地执行                                                                              |
| **三方生态A**  | 巨信       | `get_contacts`、`send_message`、`share_plan_card`                                                                                                                                                                                                      | 三方应用，标准化接口，小艺Claw经用户授权后调用                                                                        |
|            | YouLink  | `get_work_schedule`、`check_leave_status`、`auto_reply`、`update_status`                                                                                                                                                                                | 同上                                                                                               |
| **方舟矩阵生态** | 大德地图     | `search_route`、`traffic_now`、`nearby_search`、`create_navigation`                                                                                                                                                                                     | 矩阵数据已上云，统一认证共享用户画像；这些 Skill 由方舟自有Agent封装后开放给小艺Claw，执行在云端                                         |
|            | 花瓣音乐     | `get_user_playlists`、`create_travel_radio`                                                                                                                                                                                                           | 同上                                                                                               |
|            | 小众点评     | `search_poi`、`get_reviews`、`create_collection`、`post_review`                                                                                                                                                                                         | 同上                                                                                               |
|            | 行旅纵横     | `search_tickets`、`book_ticket`、`subscribe_delay`、`get_my_trips`                                                                                                                                                                                      | 同上                                                                                               |
|            | **方舟旅迹** | **`trip_list`** — 获取用户所有旅行计划列表<br>**`trip_add`** — 创建包含完整日程的新旅行计划<br>**`trip_update`** — 更新指定旅行的日程或属性<br>**`trip_delete`** — 删除旅行计划<br>**`plan_skill`** — **元技能**，不直接生成旅行计划，而是向小艺Claw暴露旅行规划的标准步骤模版与参数校验规则，辅助小艺Claw串联各生态Skill，最终通过 `trip_add` 写入方舟旅迹。 | 所有原子 Skill 由**云端方舟自有Agent**实现，直接操作矩阵内旅行数据；`plan_skill` 仅做轻量指引，不直接导流给Agent执行黑盒规划，真正决策与编排由小艺Claw完成 |

---

## 三、全旅程用户场景故事（体现意图框架协作）

用户**小李**的旅行，由小艺Claw主导规划，方舟旅迹的原子技能负责存储与管理，而行前与旅途中的主动服务通过**意图框架 + 桌面卡片 + 方舟旅迹内闭环**优雅完成。

### 场景1：一句话启程 —— 小艺Claw的跨生态编排 + trip_add 落地

小李唤醒系统小艺Claw：  
_“帮我规划下周一从北京出发去南京，三天两夜，多玩多吃，不怕走路，预算不用省。”_

**小艺Claw的调度过程（屏幕右侧显示调用链瀑布）**：

1. 调用**YouLink** `get_work_schedule` → 发现周一上午10点有站会。
2. 调用**YouLink** `check_leave_status` → 已请年假，会议可移动参加。
3. 调用**方舟旅迹** `plan_skill` → 获取“3天2夜城市美食步行游”的规划步骤模板。
4. 按模板提示，小艺Claw自主并行调度：
   - **行旅纵横** `search_tickets` → 推荐周一12:00 G123次。
   - **备忘录** `search_notes("南京")` → 命中“李记清真馆锅贴”“颐和路骑行”。
   - **小众点评** `search_poi` → 筛出总统府、科巷、老门东等高评分步行可达点。
   - **大德地图** `search_route` → 返回每日步行串联路线及预估步数。
5. 小艺Claw组装出完整时间轴，调用**方舟旅迹** `trip_add` 创建旅行计划。
6. 同时调用**YouLink** `update_status` 完成跨生态落地。

小李面前弹出时间轴卡片，每条标注数据出处。确认后，方舟旅迹出现完整行程。

### 场景2：行前一夜 —— 意图卡片触发，进入方舟旅迹闭环服务

出发前晚20:00，**云端方舟自有Agent** 根据行程时间自动触发“行前准备”内部事件。  
但它不能主动调度小艺Claw或操作系统应用。于是云端Agent执行唯一允许的对外动作：**向系统意图框架分发一条出行意图**。

- 意图类型：`TRAVEL_PREP_CHECK`
- 内容摘要：“明天南京之旅即将出发，检测到高铁票、会议冲突、打包清单等事项待确认。”

**手机负一屏/桌面出现一张舟旅卡片**：

- 标题：“南京之旅明日出发，点击查看行前准备”
- 附带关键信息预览：高铁 G123 检票口、YouLink 周一早会冲突、备忘录打包清单待核对。

小李点击卡片，**直接进入《方舟旅迹》App 的行前准备页**。

- App 内，**端侧小艺Agent** 被自动调起，并以欢迎语开启对话：“明天就要出发啦，我帮你梳理了行前清单。”
- 它通过 A2A 向云端方舟Agent索取数据后，用对话卡片逐条呈现：
  - “G123次 北京南→南京南，12:00发车，检票口A12，建议10:30出发。”
  - “检测到明早10:00有YouLink站会，建议设置高铁参会模式。需要我帮你生成提醒话术吗？”（此时仅提供建议文本，无法直接设闹钟或改状态）
  - “你的打包清单已从备忘录同步，要来一次出发前核对吗？”
- 若小李确认需要提醒，端侧Agent会生成一段自然语言指令：“提醒我明天10点高铁参会，并打开勿扰模式”，并附带“复制”按钮，用户可一键复制后粘贴给小艺Claw执行。

**亮点**：全程未跳出方舟矩阵和意图框架权限边界，跨生态操作留给用户和小艺Claw完成，但方舟旅迹内提供了高度智能的决策支持与话术生成。

### 场景3：旅途中的实时重规划 —— 延误意图卡片，App内自动改签引导

周二中午，南京暴雨。**云端方舟自有Agent** 同步感知：

- **行旅纵横** 推送延误预警：返程G124预计晚点60分钟。
- **大德地图** 实时路况：去南站拥堵。

云端Agent在矩阵内部立刻执行重规划：调用大德地图切换地铁方案，用小众点评筛选雨中室内美食，并通过 `trip_update` 更新方舟旅迹内的当日行程。  
同时，它向意图框架分发一条 **`TRAVEL_DELAY_ALERT`** 意图。

**手机桌面弹出新的舟旅卡片**：

- 标题：“返程高铁G124晚点60分钟，行程已自动调整”
- 摘要：“已切换地铁前往南站，今晚美食改至室内小吃街。点击查看详情。”

小李点击卡片，**再次进入方舟旅迹 App**。

- 端侧小艺Agent 自动展示调整后的行程对比（原计划 vs 新计划），并以对话气泡说明变更理由。
- 对于需要跨生态协作的部分，Agent提供主动帮助：
  - “检测到你和好友老王约了今晚的饭，需要通知他时间变更吗？我可以帮你生成消息文本。”
  - 点击“生成消息”，自动填入：“老王，高铁晚点啦，约饭延后至19:30，地点新街口XX店。” 附带“复制”按钮，用户可粘贴至巨信发送。
  - “明日下午的YouLink会议，建议将状态改为‘差旅延误，线上接入’。已为你生成状态文案，可复制到YouLink操作。”
- 若涉及**高铁改签**等需用户手动操作的动作，端侧Agent可直接在应用内调起方舟旅迹内嵌的改签引导界面（通过云端方舟Agent与行旅纵横打通），自动填写原车次与新推荐车次，用户仅需点击确认，矩阵内即可完成改签，无需切换App或调用小艺Claw。

**亮点**：延误响应完全在矩阵内闭环，重规划直接生效；跨生态协作仅提供智能文本生成，由用户自行粘贴操作，边界清晰且体验流畅。

### 场景4：旅行记忆沉淀 —— 矩阵内闭环 + 系统写入

旅程结束当晚，**云端方舟自有Agent**自动：

- 在**小众点评**补评价，更新用户画像。
- 生成“南京旅行记忆”数据包。

端侧小艺Agent 同样通过卡片引导用户进入方舟旅迹，展示记忆预览，并提供“存入备忘录”的智能指令文本（如“帮我将这篇南京旅行记忆保存到备忘录”），用户可一键复制，向小艺Claw说出或粘贴即可完成最终跨生态写入。

---

## 四、「舟旅」AI Agent 核心能力设计（适配新约束）

1. **双层Agent分工与意图框架协作**
   
   - **云端方舟自有Agent**：掌握矩阵全部数据，具备领域深智能，负责矩阵内行程管理、延误响应等自主操作。对外**只通过意图框架分发标准出行意图**，不直接调用系统或三方应用。
   - **端侧小艺Agent**：运行在方舟旅迹内，通过A2A与云端Agent联动，为用户提供行前核对、延误方案呈现、改签引导、智能话术生成等服务。所有跨生态操作均转化为可复制的自然语言建议。
   - **系统小艺Claw**：跨生态超级编排者，负责初始规划时的全局调用，以及后续由用户主动发起的跨生态执行（如粘贴指令后执行的闹钟设置、消息发送等）。

2. **意图卡片与方舟旅迹闭环**
   任何行程变动（行前提醒、延误、突发天气）均由云端Agent生成意图，以桌面卡片形式触达用户。点击卡片直达方舟旅迹对应上下文页面，端侧Agent自动激活并提供解决方案，形成“卡片感知 → 一键进入 → App内闭环处理”的体验。

3. **智能文本生成与用户主导的跨生态执行**
   所有超出矩阵权限的操作，方舟旅迹 Agent 提供精准的自然语言指令或消息文本，用户通过复制、粘贴或口述给小艺Claw完成，保证权限边界清晰，同时体验丝滑。

4. **安全沙箱与隐私边界**
   系统与三方应用数据仅在小艺Claw被显式调用时读取；矩阵内数据在云端沙箱闭环处理；端侧Agent不持有非矩阵数据；意图卡片仅展示脱敏摘要，充分保护隐私。

---

## 五、高光 Demo 脚本（6分钟，突出新闭环机制）

**【0:00-0:30】 架构速览**

- 投屏三层架构，重点说明“意图卡片”和“方舟旅迹闭环”机制。

**【0:30-2:00】 小艺Claw编排规划**

- 语音输入需求，屏幕显示调用链，突出 `plan_skill` 元模板和 `trip_add` 落地。
- Toast 提示YouLink同步完成。

**【2:00-3:00】 行前卡片与闭环核对**

- 演示手机桌面弹出舟旅卡片“南京之旅明日出发”。
- 点击卡片进入方舟旅迹，端侧Agent自动开启对话，呈现检票口、会议冲突、打包清单。
- 点击“生成提醒话术”，复制指令，粘贴至小艺Claw演示（可选），强调用户主动权。

**【3:00-4:30】 延误重规划与智能引导**

- 模拟弹窗：卡片“返程高铁G124晚点60分钟”。
- 点击卡片进入方舟旅迹，端侧Agent展示行程对比和地铁新方案。
- 点击“通知老王”，生成消息文本，演示复制后粘贴到巨信发送。
- 演示矩阵内改签引导：App内显示原车次与新推荐，用户点击确认，即时完成改签。

**【4:30-5:30】 记忆沉淀**

- 展示旅程结束后的卡片“南京旅行记忆已生成”。
- 点击进入方舟旅迹，Agent展示记忆长文，点击“复制保存指令”，粘贴给小艺Claw存入备忘录，弹出备忘录成功界面。
- 画像更新展示。

**【5:30-6:00】 总结**

- 强调：跨生态规划由小艺Claw完成；行中主动服务通过意图卡片+方舟旅迹闭环；用户始终掌握跨生态操作控制权，隐私与权限边界清晰。
